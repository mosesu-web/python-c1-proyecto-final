{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Cita",
    "description": "Schema for validating appointment creation for an admin role",
    "type": "object",
    "properties": {
        "fecha": {
            "type": "string",
            "description": "fecha de la cita",
            "format": "date"
        }
    },
    "required": ["fecha"],
    "additionalProperties": false
}